services:
  - type: web
    name: creative-story-bot
    runtime: python
    runtimeVersion: 3.11
    buildCommand: pip install -r requirements.txt
    startCommand: python app_web.py
    envVars:
      - key: FLASK_ENV
        value: production
      - key: GROQ_API_KEY
        scope: secret
