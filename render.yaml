services:
  - type: web
    name: creative-story-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app_web.py
    envVars:
      - key: FLASK_ENV
        value: production
